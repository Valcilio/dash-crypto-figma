Updates on use cases, connectors and external factors layers, excluded one class from entities layer.

Warning:
    - Just entities layer's complete, the others need to be completed.

Additioned:
    - domain/use_cases/questioner.py: created to get data for do the requests in the API.
    - domain/connectors/apresentator.py: created to connect plots and filters for the framework.
    - domain/connectors/datagetter.py: created to do the requests on the API and get the data.

Revised:
    - domain/entities: excluded one of the classes because it was useless.